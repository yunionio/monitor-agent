# Deploy monitor agent in VirutalMachine created by Yunion OneCloud

## Usage

modify `inventory/hosts.yaml` and `config.yaml`

execute `ansible-playbook -i inventory/hosts.yaml -e @config.yaml playbook.yaml`
