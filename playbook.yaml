- hosts: all
  become: true

  vars:
    server_type: guest
    node_exporter_version: 1.6.1

  tasks:
    - name: "Deploy monitor agent for Guest"
      include_role:
        name: node_exporter
      when: 
        - server_type == "guest"
        - ansible_facts['os_family'] != "Windows"

